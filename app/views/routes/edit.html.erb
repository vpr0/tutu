<h1>Изменить маршрут</h1>

<%= form_with(model: @route, local: true) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this rou from being saved:</h2>

      <ul>
        <% @route.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %>
    <%= f.number_field :number, id: :train_number %>
  </div>

  <div class="field">
    <%= f.label :railway_station_ids, "Текущая станция" %>
    <%= f.collection_select :railway_station_ids, RailwayStation.all, :id, :title, {}, { multiple: true } %>
  </div>

  <div class="actions">
    <%= f.submit 'Изменить' %>
  </div>
<% end %>

<%= link_to 'Назад', routes_path %>
